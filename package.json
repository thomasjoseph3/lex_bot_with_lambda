{
  "name": "lex-claude-qa-bot",
  "version": "1.0.0",
  "description": "Lex Bot with Claude Q&A using AWS Bedrock",
  "main": "index.js",
  "dependencies": {
    "@aws-sdk/client-bedrock-agent-runtime": "^3.x.x",
    "@aws-sdk/client-bedrock-runtime": "^3.x.x",
    "serverless-dotenv-plugin": "^6.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless-api-gateway-throttling": "^2.0.3"
  }
}
